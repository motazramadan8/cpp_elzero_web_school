/* -----------------------------------------------------------
-- int => My code will return int (number)
-- {} => Container for block of code
-- cout => character output
-- << => stream insertion operator
-- "" => string
-- \n => special character (Escaping character) to add newline
----------------------------------------------------------- */

#include <iostream>

int main()
{
  std::cout << "Line 1\n";
  std::cout << "Line 2\n";
  std::cout << "Line 3\n";
  std::cout << "std::cout"; // print "std::cout" and never use it as programming statement because it is string
  return 0;
}
